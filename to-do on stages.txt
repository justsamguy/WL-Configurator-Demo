add an individual line for the crate/packaging costs instead of including it in the shipping estimator. put this line with the shipping accessories so it shows/hides with them, but at the top of the list. style like the accessories are styled
add a horizontal-center-center (text & field) description field for each shipping accessory line.


Overhaul media to improve loading times

use the output system to make popular designs as design presets, so the user can navigate all customization stages with pre-selected options ready to customize - existing tiles remain in a section below that uses the existing one-stage-at-a-time approach.

branch and code mobile website

fork and rebuild entire app to use react code