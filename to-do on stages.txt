use assistant font
make the price summary tooltip a little prettier

add warning for shipping glass


Overhaul media to improve loading times

Add popular designs to as configured presets, so the user can navigate all customization stages with pre-selected options ready to customize - existing tiles remain in a section below that uses the existing one-stage-at-a-time approach.


create a csv-friendly export option
add the browser warning to save progress

remove all instances in the codebase where an !important or other similar override is being used and replace them with their proper intended designs.

fork and rebuild entire app to use react code